{
  "input_method": "stdin",
  "parameters": {
    "reboot": {
      "description": "Should the server reboot after patching has been applied? (Defaults to false)",
      "type": "Optional[Boolean]"
    },
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Carry out OS patching on windows servers, optionally including a reboot"
}
